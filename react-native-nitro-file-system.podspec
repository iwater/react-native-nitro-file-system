require "json"

package = JSON.parse(File.read(File.join(__dir__, "package.json")))

Pod::Spec.new do |s|
  s.name         = "react-native-nitro-file-system"
  s.version      = package["version"]
  s.summary      = package["description"]
  s.homepage     = "https://github.com/iwater/react-native-nitro-file-system"
  s.license      = package["license"]
  s.authors      = package["author"]
  
  s.platform     = :ios, "13.0"
  s.source       = { :git => "https://github.com/iwater/react-native-nitro-file-system.git", :tag => "v#{s.version}" }
  
  s.source_files = [
    "ios/**/*.{h,m,mm,swift}",
    "cpp/**/*.{hpp,cpp}"
  ]
  
  s.dependency "React-Core"
  # s.dependency "react-native-nitro-modules"
  
  # Add vendored xcframework (Rust binary)
  s.vendored_frameworks = "ios/Frameworks/RustFileSystem.xcframework"
  
  s.pod_target_xcconfig = {
    "HEADER_SEARCH_PATHS" => [
      "\"$(PODS_ROOT)/react-native-nitro-modules/ios\"",
      "\"$(PODS_ROOT)/Headers/Public/react-native-nitro-modules\"",
      "\"$(PODS_TARGET_SRCROOT)/nitrogen/generated/shared/c++\"",
      "\"$(PODS_TARGET_SRCROOT)/nitrogen/generated/shared\"",
      "\"$(PODS_TARGET_SRCROOT)/nitrogen/generated/ios/c++\"",
      "\"$(PODS_TARGET_SRCROOT)/nitrogen/generated/ios\"",
      "\"$(PODS_TARGET_SRCROOT)/cpp\"",
      "\"$(PODS_TARGET_SRCROOT)/ios/Frameworks/RustFileSystem.xcframework/ios-arm64/RustFileSystem.framework/Headers\"",
      "\"$(PODS_TARGET_SRCROOT)/ios/Frameworks/RustFileSystem.xcframework/ios-arm64_x86_64-simulator/RustFileSystem.framework/Headers\""
    ],
    "OTHER_SWIFT_FLAGS" => "-cxx-interoperability-mode=default"
  }

  # Add all files generated by Nitrogen
  load 'nitrogen/generated/ios/RNNodeFileSystem+autolinking.rb'
  add_nitrogen_files(s)
end
